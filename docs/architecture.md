# Rearview architecture

Rearview is a TypeScript monorepo with shared review schemas, a CLI, a local server, and web UI modules. The local app treats `.rearview/` as the default human-readable metadata directory.
